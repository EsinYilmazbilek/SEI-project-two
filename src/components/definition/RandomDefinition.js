// import { useParams } from 'react-router'
// import React from 'react'


// function HandleRandomWord() {
//   const [randomWord, setRandomWord] = React.useState(null)
//   const { userWord } = useParams()

//   const randomWord = setRandomWord(Math.floor(Math.random() * words.length + 1))
//   return randomWord
// }
// console.log(randomWord)
// console.log(HandleRandomWord)

// export default HandleRandomWord